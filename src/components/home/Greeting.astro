---
export interface Props {
  name: string;
  emoji?: string;
}

const { name, emoji = '👋' } = Astro.props;
---

<h1 class="text-2xl md:text-3xl font-bold text-center pt-8">
  ¡Hola {name}! <span aria-hidden="true">{emoji}</span>
</h1>
